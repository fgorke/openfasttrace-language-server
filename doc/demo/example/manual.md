# Coffee Maker — User Manual

<!-- [uman->req~cleaning-reminder~1] -->
Descale the machine when the cleaning light comes on. Use one cleaning tab,
run two brew cycles with water only, then rinse the tank.

This file shows that coverage does not have to live in source code: the tag in
the Markdown comment above covers `req~cleaning-reminder~1` with the artifact
type `uman` (user manual).
