# The server communicates over stdout (LSP JSON-RPC), so logging must never write there.
writer=console
writer.stream=err
writer.format={date: HH:mm:ss.SSS} [{level}] {class}.{method}(): {message}

level=info
