<idea-plugin>
    <id>org.itsallcode.openfasttrace.lsp</id>
    <name>OpenFastTrace Language Server</name>
    <vendor>Felix Gorke</vendor>
    <description><![CDATA[
        Integrates the <a href="https://github.com/itsallcode/openfasttrace">OpenFastTrace</a>
        (OFT) language server into the IDE via the built-in LSP client. The server understands
        OFT specification items and coverage tags and provides:
        <ul>
            <li><b>Hover documentation</b>: title and description of the referenced specification item</li>
            <li><b>Go to definition</b>: from a coverage tag to the specification item and from an item to all its covering tags</li>
            <li><b>Find references</b>: all coverage tags in the workspace that cover a specification item</li>
            <li><b>Trace diagnostics</b>: uncovered specification items, links to items that do not exist and duplicate definitions, reported for every file in the project and listed under <i>Project Errors</i></li>
            <li><b>Quick fix</b>: one-click update of an outdated coverage tag revision, for a single tag or for every outdated reference to the item at once</li>
            <li><b>Rename</b>: renames a specification item together with every coverage tag and reference in the workspace</li>
            <li><b>Trace report</b>: renders a full OpenFastTrace report through <i>Tools | Generate OpenFastTrace Report</i> and opens it in the editor</li>
            <li><b>Coverage code lens</b>: a summary above each specification item naming the artifact types it still needs and the artifact types already covering it</li>
            <li><b>Coverage hierarchy</b>: the full coverage chain as a type hierarchy (Ctrl+H), always starting at the top and reaching down to the coverage tags in source. Opened on a specification item ID</li>
            <li><b>Symbol search</b>: finds specification items through Search Everywhere</li>
            <li><b>Syntax highlighting</b>: specification item definitions, section keywords and coverage tags via semantic tokens</li>
            <li><b>Code completion</b>: specification item IDs in <code>Covers:</code> sections and coverage tag targets</li>
            <li><b>Tag skeletons</b>: pressing Ctrl+Space in a comment offers a skeleton such as
                <code>[impl-&gt;...]</code> for every artifact type the workspace needs, including project specific ones</li>
            <li><b>Ignore file</b>: glob patterns in a <code>.oftignore</code> file in the project root
                exclude files from all OFT features, on top of the build output and hidden paths
                that are excluded by default</li>
        </ul>
    ]]></description>
    <change-notes><![CDATA[
        <ul>
            <li>0.4: no separate Java installation needed, coverage hierarchy,
                coverage code lens, trace report generation, and OpenFastTrace updated to 4.9.0</li>
            <li>0.3: trace defects reported as diagnostics, rename across the workspace,
                symbol search, and OpenFastTrace updated to 4.8.0</li>
            <li>0.2: released new VS Code extension</li>
            <li>0.1: initial release with navigation, hover, diagnostics, quick fix,
                semantic highlighting and completion backed by the OFT language server</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.lsp</depends>

    <extensions defaultExtensionNs="com.intellij">
        <platform.lsp.serverSupportProvider
                implementation="org.itsallcode.openfasttrace.lsp.intellij.OftLspServerSupportProvider"/>
        <postStartupActivity
                implementation="org.itsallcode.openfasttrace.lsp.intellij.OftLspStartupActivity"/>
        <completion.confidence language="any" order="first"
                implementationClass="org.itsallcode.openfasttrace.lsp.intellij.OftCompletionConfidence"/>
    </extensions>

    <actions>
        <action id="Oft.GenerateTraceReport"
                class="org.itsallcode.openfasttrace.lsp.intellij.OftGenerateTraceReportAction"
                text="Generate OpenFastTrace Report..."
                description="Renders a trace report for the workspace and opens it in the editor">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
